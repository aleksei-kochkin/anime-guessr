# Anime Guessr 🎌

Игра для угадывания аниме, фильмов и сериалов по изображениям, вдохновлённая [framed.wtf](https://framed.wtf/).

## 🚀 Быстрый старт

```bash
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

### Настройка TMDB API (для фильмов и сериалов)

1. Зарегистрируйтесь на [The Movie Database (TMDB)](https://www.themoviedb.org/signup)
2. Получите API ключ в [настройках API](https://www.themoviedb.org/settings/api)
3. Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_TMDB_API_KEY=your_api_key_here
```

**Примечание:** Игра будет работать и без TMDB API ключа, но только в режиме Anime. Для работы с Movies и TV Shows необходимо добавить ключ.

## 🎮 Как играть

1. У вас есть **6 попыток** чтобы угадать аниме, фильм или сериал
2. Каждая попытка **раскрывает новый скриншот**
3. **Клик по индикатору попытки** переключает между раскрытыми скриншотами
4. Вводите название (русское или английское) или нажмите **Skip** для пропуска
5. Чем быстрее угадаете - **больше очков** (6, 5, 4, 3, 2, 1)
6. **Предыдущие попытки** отображаются слева
7. **Переключайте режим** между Anime, Movies и TV Shows в верхней части экрана

## ✨ Особенности

- **Три режима** - угадывайте аниме, популярные фильмы или сериалы
- **6 попыток** - как в Framed.wtf, новый скриншот каждую попытку
- **Перемешанные кадры** - скриншоты каждый раунд в случайном порядке
- **Интерактивные индикаторы** - кликайте по точкам для переключения кадров
- **Боковые панели** - предыдущие попытки слева, интерфейс не прыгает
- **Система очков** - больше очков за быстрый ответ
- **Точная проверка** - выбор из списка проверяется по ID (100% точность)
- **Автодополнение** - живой поиск по Shikimori API (аниме) и TMDB API (фильмы/сериалы)
- **Умная проверка** - ручной ввод допускает частичное совпадение
- **Фильтры аниме** - настраивайте тип, статус, сезон, рейтинг и возрастную категорию
- **Сохранение прогресса** - фильтры и выбранный режим сохраняются в cookies (доступны на сервере), статистика в localStorage
- **Правильная загрузка** - при перезагрузке страницы сразу загружается последний выбранный режим
- **Адаптивный дизайн** - темная/светлая темы, mobile-first

## 🎯 Фильтры

### Anime режим

Нажмите кнопку "Filters" в верхней части экрана, чтобы настроить параметры выбора аниме:

- **Тип (Type)**: TV, Movie, OVA, ONA, Special, Music
- **Статус (Status)**: Released, Ongoing, Announced
- **Возрастной рейтинг (Age Rating)**: G, PG, PG-13, R, R+
- **Сезон (Season)**: Формат "2020_2024", "summer_2023" и т.д.
- **Минимальный балл (Score)**: От 0 до 10
- **Длительность эпизода (Duration)**: Короткие (<10 мин), Средние (<30 мин), Полные (>30 мин)

### Movies и TV Shows режимы

Для фильмов и сериалов доступны следующие фильтры:

- **Минимальный рейтинг (Minimum Rating)**: Слайдер от 0 до 10
- **Год выпуска (Year Range)**: Диапазон от и до
- **Страна (Country)**: Выбор одной страны из популярных (США, Великобритания, Франция, Германия, Япония, и др.)
- **Жанр (Genre)**: Выбор одного жанра (загружается динамически из TMDB)

**Примечание:** Для фильмов и сериалов можно выбрать только одну страну и один жанр (особенность TMDB API).

Все фильтры автоматически сохраняются и применяются к последующим раундам.

## 📊 Статистика

Ваш прогресс автоматически сохраняется:

- **Round** - текущий раунд
- **Score** - общий счет (максимум 6 очков за раунд)
- **Accuracy** - процент правильно угаданных аниме/фильмов/сериалов

Кнопка **Reset Stats** позволяет начать заново и сбросить всю статистику.

## 🎬 Источники контента

- **Anime** - данные из [Shikimori](https://shikimori.one) (бесплатно, без регистрации)
- **Movies & TV Shows** - данные из [The Movie Database (TMDB)](https://www.themoviedb.org) (бесплатная регистрация для получения API ключа)

## 🛠️ Технологии

- Next.js 16 (App Router)
- TypeScript (strict mode)
- Tailwind CSS 4
- Shikimori API (аниме)
- TMDB API (фильмы и сериалы)

## 📁 Структура

```
app/              # Next.js routes
components/       # React компоненты
  ├── game/
  │   ├── UniversalFilterPanel.tsx  # Универсальный компонент фильтров
  │   └── ...
lib/
  ├── api/        # API клиенты (Shikimori, TMDB)
  ├── actions/    # Server Actions
  ├── types/      # TypeScript типы
  ├── utils/      # Утилиты
  └── constants/  # Конфигурация
      ├── game.ts    # Основные константы игры
      └── filters.ts # Конфигурация фильтров (JSON-подобная структура)
```

## 🎛️ Система фильтров

Приложение использует универсальный компонент фильтров, настраиваемый через JSON-конфигурацию (`lib/constants/filters.ts`):

### Типы фильтров:
- `button-multi` - множественный выбор кнопками
- `button-single` - одиночный выбор кнопками  
- `dynamic-buttons` - кнопки с динамической загрузкой данных
- `slider` - слайдер для числовых значений
- `text` - текстовое поле
- `number-range` - диапазон чисел (от-до)
- `select` - выпадающий список

Это позволяет легко добавлять новые типы фильтров без дублирования кода.

## 📝 Лицензия

MIT
